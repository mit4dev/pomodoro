import { LocalStorageService } from './local-storage-service'

describe('local-storage-service', () => {
  const instance = new LocalStorageService()

  it.todo('returns the existing item')

  it.todo('returns null if the key does not exist')

  it.todo('returns null if value cannot be parsed')
})
