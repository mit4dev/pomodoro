import { HistoryService } from './history-service'

describe('history-service', () => {
  const instance = new HistoryService()

  it.todo('returns empty array when no history present')

  it.todo('returns the timer history with actions')
})
